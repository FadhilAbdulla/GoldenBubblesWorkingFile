<html lang='en'>
  <head>
    <meta charset='UTF-8' />
    <meta name='viewport' content='width=device-width, initial-scale=1.0' />
    <title>Document Verification</title>
    <style>
      body {
        font-family: Arial, sans-serif;
        line-height: 1.6;
        color: #333;
        max-width: 600px;
        margin: 0 auto;
        padding: 20px;
      }
      .header {
        background-color: #ffc107;
        color: #000;
        padding: 20px;
        text-align: center;
        border-radius: 5px 5px 0 0;
      }
      .content {
        background-color: #f8f9fa;
        padding: 30px;
        border-radius: 0 0 5px 5px;
      }
      .status {
        padding: 15px;
        margin: 20px 0;
        border-radius: 5px;
        font-weight: bold;
      }
      .status.success {
        background-color: #d4edda;
        color: #155724;
        border: 1px solid #c3e6cb;
      }
      .status.pending {
        background-color: #fff3cd;
        color: #856404;
        border: 1px solid #ffeaa7;
      }
      .status.rejected {
        background-color: #f8d7da;
        color: #721c24;
        border: 1px solid #f5c6cb;
      }
      .footer {
        text-align: center;
        margin-top: 20px;
        font-size: 12px;
        color: #666;
      }
    </style>
  </head>
  <body>
    <div class='header'>
      <h1>Document Verification Update</h1>
    </div>
    <div class='content'>
      <h2>Hello {{firstName}} {{lastName}},</h2>

      <p>We have an update regarding your document verification.</p>

      <div class='status {{statusClass}}'>
        Status:
        {{status}}
      </div>

      {{#if message}}
        <p><strong>Message:</strong> {{message}}</p>
      {{/if}}

      {{#if documentsProcessed}}
        <p><strong>Documents Processed:</strong></p>
        <ul>
          {{#each documentsProcessed}}
            <li>{{this}}</li>
          {{/each}}
        </ul>
      {{/if}}

      {{#if nextSteps}}
        <p><strong>Next Steps:</strong> {{nextSteps}}</p>
      {{/if}}

      <p>If you have any questions, please contact our support team.</p>

      <p>Best regards,<br />The {{appName}} Team</p>
    </div>
    <div class='footer'>
      <p>&copy; 2025 {{appName}}. All rights reserved.</p>
    </div>
  </body>
</html>